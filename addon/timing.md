[« Back to Docs](./)


# Baremark Addon: `timing.js`

This addon installs a tiny wrapper function, which for each call to
`baremark()` outputs (to the developer console) how the Markdown → HTML
conversion took (in milliseconds). To use, add:

```
import 'timing.js'
```

<!--[eof]-->
