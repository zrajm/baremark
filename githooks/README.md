To activate these Git hooks (for only this repository), either run:

```
git config core.hooksPath githooks
```

…or copy them all to Git's hook directory using:

```
cp -a githooks/* .git/hooks/
```

<!--[eof]-->
